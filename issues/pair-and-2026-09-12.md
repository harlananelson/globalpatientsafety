# Honor drug= AND event= (pair isolation)

Handshake for `/projects/faers-mobi/`. Do not merge this GPS PR. Deploy when good. Pay gate / billing is out of scope.

#33 sort= is live. Do not pile this onto #33.

## Problem

`GET /signals?drug=tzield&event=Nausea` ignores `drug=` (event path wins) and returns every Nausea pair (~2459). Backend `aggregate_pairs` already supports AND.

## Wanted

1. When both `drug=` and `event=` are set, AND-isolate with the same fuzzy isolation on each side (JSON + CSV).
2. Single-param `drug=` or `event=` stays as today.
3. Document in `API.md`. Optional UI: refine event on a drug profile (or drug on an event search) if cheap.
4. Keep isolation. Event path stays 200.

## Done when

- `GET /signals?drug=tzield&event=Nausea` returns only tzield/teplizumab × Nausea (isolated names), still 200.
- CSV same. Live on https://faers.mobi. Deploy note with verify URLs.

## Out of scope

Pay gate, Stripe, period-picker, class-wide temporal, merging this GPS PR.
