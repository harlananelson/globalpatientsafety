# Monitor alert 20260915-2236: faers.mobi check failures

Handshake. Filed automatically by `scripts/monitor_faers_mobi.sh` (globalpatientsafety, cron on the workstation).
Implement the fix in `/projects/faers-mobi/`, recycle the layer that owns it, and post live body numbers here.
Do not merge; the owner seat merges after the monitor comments "Recovered".

## Failing checks (1 of 27) at 2026-09-15T22:36:42-04:00

| check | request | result |
|---|---|---|
| `api-html-slash` | `GET /api/` | status 500 (want 200) |

## Passing

26 checks passed on the same run, each retried once after 20 s before being counted.

## Which layer (from the implementer handoff)

| Symptom | Layer | Recycle |
|---|---|---|
| `/signals` JSON hangs or 0 bytes | cheap JSON `:3841` | `systemctl restart faers-signals-api` |
| brief / pdf / profile / class / series hang | heavy `:3843` | `systemctl restart faers-signals-heavy` |
| static `/api`, `/api.md`, `/llms.txt`, `/openapi.json`, bot homepage, `/mcp` | nginx | `nginx -t && systemctl reload nginx` |
| homepage HTML / banner / head | Shiny UI | `restart.txt`; kill only the SockJS whose cwd is the faers-mobi dir |

## Keep

- No new `/signals` params, no pay gate, no `/api/v1/`
- Fix the cause, not the check: if a check is wrong, say so here and the owner seat edits the monitor
