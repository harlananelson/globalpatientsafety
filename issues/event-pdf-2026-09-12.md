# Event-only format=pdf

Handshake for `/projects/faers-mobi/`. Do not merge this GPS PR. Deploy when good. Pay gate / billing is out of scope.

#58 Event-only format=brief is live. Do not pile this onto #58.

## Problem

Drug and pair `format=pdf` are live. Event-only `format=brief` is now 200 markdown, but event-only `format=pdf` is still missing (or 400). Reviewers cannot download an AE-centric RWE PDF.

## Wanted

`GET /signals?event=&format=pdf` returns `application/pdf` with the same content as event `format=brief` (mirror drug #54).

1. Event required. Drug-only / pair PDF unchanged.
2. Same Typst path if already in the stack. Include disclaimer and Data through.
3. Document in API.md + openapi.json as needed.
4. Event-only UI can still wait.

Example: `GET /signals?event=Nausea&format=pdf`

No new compute. No billing / pay gate.

## Done when

- That URL is 200 application/pdf matching the event brief (Nausea: 2459 pairs, top n otezla, Data through 2025Q4).
- Drug-only / pair PDF unchanged. Documented. Deploy note with the URL.

## Out of scope

Pay gate, Stripe, site chat box, event-only UI, new statistical compute, merging this GPS PR.
