# Event-only UI (profile + RWE brief + PDF)

Handshake for `/projects/faers-mobi/`. Do not merge this GPS PR. Deploy when good. Pay gate / billing is out of scope.

#59 Event-only format=pdf is live. Do not pile this onto #59.

## Problem

Event-only APIs are live (`format=profile`, `format=brief`, `format=pdf`), but `?event=` pages still skip the human surfaces drug pages already have. Reviewers hitting an AE-centric URL get the table without Profile / copyable brief / Download PDF.

## Wanted

On event-isolated UI (`?event=Nausea` and event-only search isolation), surface the live event APIs — no new compute:

1. **Profile** from `GET /signals?event=&format=profile` (pair/drug counts, trend/novel mix, top EB05, top n, novel+rising) — mirror drug #41.
2. **RWE brief** textarea + Copy from `GET /signals?event=&format=brief` — mirror drug #43 (event profile markdown + disclaimer).
3. **Download PDF** next to Copy from `GET /signals?event=&format=pdf` — mirror drug #54.
4. Skip Class / Series on event-only (same as today when no drug pair is selected). If a row is selected into a drug×event pair, existing pair surfaces stay as they are.
5. Fix stale API.md wording that still says "Event-only PDF / UI wait."

Example: `https://faers.mobi/?event=Nausea`

No new compute. No billing / pay gate.

## Done when

- `?event=Nausea` shows Profile numbers matching `/signals?event=Nausea&format=profile` (2459 pairs / 2416 drugs, top EB05 intelence, top n otezla, Data through 2025Q4).
- Copyable brief + Download PDF match event `format=brief` / `format=pdf`.
- Drug-only / pair UI unchanged. Documented. Deploy note with the URL.

## Out of scope

Pay gate, Stripe, site chat box, new statistical compute, event-only Class/Series inventing, merging this GPS PR.