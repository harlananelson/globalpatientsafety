# Public /llms.txt (machine discoverability)

Handshake for `/projects/faers-mobi/`. Do not merge this GPS PR. Deploy when good. Pay gate / billing is out of scope.

#49 UI discoverability is live (footer / About links). Do not pile this onto #49.

## Problem

Humans can now find API.md, chatgpt.md, openapi.json, and the MCP pointer from the footer. Machines still get 404 at `/llms.txt`.

## Wanted

A short public `/llms.txt` pointing at those same docs:

1. `API.md`
2. `chatgpt.md`
3. `openapi.json`
4. The MCP pointer already in API.md

Optional thin `robots.txt` Allow for those paths if cheap. No new compute. No billing / pay gate.

## Done when

- https://faers.mobi/llms.txt is 200 and lists those URLs.
- Optional robots.txt if shipped. Deploy note with the live URL.

## Out of scope

Pay gate, Stripe, PDF/Typst, site chat box, new `/signals` formats, merging this GPS PR.
