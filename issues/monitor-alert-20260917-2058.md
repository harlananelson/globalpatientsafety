# Monitor alert 20260917-2058: faers.mobi check failures

Handshake. Filed automatically by `scripts/monitor_faers_mobi.sh` (globalpatientsafety, cron on the workstation).
Implement the fix in `/projects/faers-mobi/`, recycle the layer that owns it, and post live body numbers here.
Do not merge; the owner seat merges after the monitor comments "Recovered".

## Failing checks (33 of 33) at 2026-09-17T20:58:51-04:00

| check | request | result |
|---|---|---|
| `home-human` | `GET /` | status 500 (want 200) |
| `home-bot-chatgpt` | `GET /` | status 500 (want 200) |
| `home-bot-perplexity` | `GET /` | status 500 (want 200) |
| `home-bot-claude` | `GET /` | status 500 (want 200) |
| `bot-home-md` | `GET /bot-home.md` | status 500 (want 200) |
| `api-html` | `GET /api` | status 500 (want 200) |
| `api-html-slash` | `GET /api/` | status 500 (want 200) |
| `api-md` | `GET /api.md` | status 500 (want 200) |
| `API-md-upper` | `GET /API.md` | status 500 (want 200) |
| `llms-txt` | `GET /llms.txt` | status 500 (want 200) |
| `openapi` | `GET /openapi.json` | status 500 (want 200) |
| `chatgpt-md` | `GET /chatgpt.md` | status 500 (want 200) |
| `mcp-md` | `GET /mcp.md` | status 500 (want 200) |
| `mcp-get` | `GET /mcp` | status 500 (want 200) |
| `mcp-initialize` | `MCP /mcp` | status 500 (want 200) |
| `mcp-server-py` | `GET /mcp/server.py` | status 500 (want 200) |
| `signals-json` | `GET /signals?drug=tzield&event=Nausea` | status 500 (want 200) |
| `signals-headers` | `HEAD /signals?event=rash&sort=n&limit=5` | status 500 (want 200) |
| `signals-series` | `GET /signals?drug=tzield&event=Nausea&format=series` | status 500 (want 200) |
| `signals-profile-drug` | `GET /signals?drug=tzield&format=profile` | status 500 (want 200) |
| `signals-profile-event` | `GET /signals?event=Nausea&format=profile` | status 500 (want 200) |
| `signals-class` | `GET /signals?drug=tzield&event=Nausea&format=class` | status 500 (want 200) |
| `signals-label` | `GET /signals?drug=tzield&event=Nausea&format=label` | status 500 (want 200) |
| `signals-brief-pair` | `GET /signals?drug=tzield&event=Nausea&format=brief` | status 500 (want 200) |
| `signals-brief-event` | `GET /signals?event=Nausea&format=brief` | status 500 (want 200) |
| `signals-pdf` | `GET /signals?drug=tzield&event=Nausea&format=pdf` | status 500 (want 200) |
| `signals-row-flags` | `GET /signals?drug=tzield&limit=1` | status 500 (want 200) |
| `signals-class-flags` | `GET /signals?drug=tzield&event=Nausea&format=class` | status 500 (want 200) |
| `signals-label-status` | `GET /signals?drug=tzield&event=Nausea&format=label` | status 500 (want 200) |
| `signals-plus-decode` | `GET /signals?event=ischaemic+stroke&limit=1` | status 500 (want 200) |
| `signals-bad-format-400` | `GET /signals?drug=tzield&event=Nausea&format=bogus` | status 500 (want 400) |
| `formats-contract-match` | `GET /openapi.json` | status 500 (want 200) |
| `well-known-404` | `GET /.well-known/ai-plugin.json` | status 500 (want 404) |

## Passing

0 checks passed on the same run, each retried once after 20 s before being counted.

## Which layer (from the implementer handoff)

| Symptom | Layer | Recycle |
|---|---|---|
| `/signals` JSON hangs or 0 bytes | cheap JSON `:3841` | `systemctl restart faers-signals-api` |
| brief / pdf / profile / class / series hang | heavy `:3843` | `systemctl restart faers-signals-heavy` |
| static `/api`, `/api.md`, `/llms.txt`, `/openapi.json`, bot homepage, `/mcp` | nginx | `nginx -t && systemctl reload nginx` |
| homepage HTML / banner / head | Shiny UI | `restart.txt`; kill only the SockJS whose cwd is the faers-mobi dir |

## Keep

- No new `/signals` params, no pay gate, no `/api/v1/`
- Fix the cause, not the check: if a check is wrong, say so here and the owner seat edits the monitor
