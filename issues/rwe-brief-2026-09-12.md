# Copyable RWE brief (profile + class + explain)

Handshake for `/projects/faers-mobi/`. Do not merge this GPS PR. Deploy when good. Pay gate / billing is out of scope.

#42 UI class peers is live. Do not pile this onto #42.

## Problem

Profile, class, and explain_signal are all on the page, but a person (or an LLM) still has to stitch them. The original bar was a usable RWE-style AE report.

## Wanted

One copyable plain-text or markdown brief on isolated `?drug=` or a selected pair.

1. Stitch live `format=profile` + `format=class` (when a pair is selected) + the existing explain_signal paragraph.
2. Always include: isolated names, n/eb05/trend/novel, class_wide if present, **hypothesis not causation**.
3. Copy button. No new compute. Event-only can skip or pair-only subset.
4. Optional: same text from MCP as `rwe_brief` if it is just a template over those payloads.

## Done when

- https://faers.mobi/?drug=tzield (and tzield×Nausea) has a copyable brief whose numbers match the live APIs.
- Disclaimer present. Live. Deploy note with those URLs.

## Out of scope

Pay gate, Stripe, PDF/Typst, site chat box, new `/signals` formats, merging this GPS PR.
